#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'This program will be using puts to print a sentence'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char str[] = '"Programming is like building a multilingual puzzle';

	puts(str);

	return (0);
}
